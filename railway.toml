[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && npx prisma generate"

[deploy]
preDeployCommand = "cd backend && npx prisma generate"
startCommand = "cd backend && node src/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
